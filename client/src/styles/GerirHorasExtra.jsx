import styled from 'styled-components';

const Wrapper = styled.section`
  .tittle {
    margin: 1rem 0 1.5rem;
  }
  .tittle h1 {
    margin: 0;
    font-size: 1.5rem;
    color: var(--grey-900);
  }

  .listaGerirHorasExtra {
    margin-top: 0.5rem;
  }
  .tiposTrabalho {
    margin-top: 0.5rem;
  }

  hr {
    border: none;
    border-top: 1px solid var(--grey-200);
    margin: 1rem 0;
  }
`;

export default Wrapper;
